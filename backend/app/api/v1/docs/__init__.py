"""User-facing OpenAPI text of the v1 API, kept out of the routers and the schemas.

`errors.py` holds the catalogue of error responses every operation can answer, `operations.py`
the summary and description of each operation and `examples.py` the example payloads of the
schemas (the ones published in `docs/api/fixtures`). All of it is written in Spanish, because
it is what a reader of `/api-docs` sees.
"""
