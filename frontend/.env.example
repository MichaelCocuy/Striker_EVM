# Base URL of the backend API (the Vite dev server proxies /api to http://localhost:8000).
VITE_API_BASE_URL=/api/v1

# When "true" the app boots a Mock Service Worker with in-memory data instead of calling the backend.
VITE_USE_MOCKS=false
